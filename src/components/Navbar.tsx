"use client";

import Header from "./Header";

const Navbar = () => {
  return (
    <nav className="bg-[var(--primary-color)] rounded-2xl m-1">
      <Header />
    </nav>
  );
};

export default Navbar;
